extends layouts/main
include mixins/videoBlock

block content
    .search__header
        h3 Searching For: #{searchingBy}
    .search__videos
        each item in videos
            +videoBlock({
                title:item.title,
                views:item.views,
                videoFile:item.videoFile
            })